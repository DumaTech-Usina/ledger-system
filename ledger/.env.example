NODE_ENV=production
SERVER_PORT=3000

# PostgreSQL — validated ledger events only
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=ledger
DB_PASSWORD=changeme
DB_DATABASE=ledger
DB_SSL=false
DB_LOGGING=false
DB_MIGRATIONS_RUN=true

# MongoDB — staging records and rejected events
MONGODB_URI=mongodb://localhost:27017/ledger

# Audit System
AUDIT_LOG_DIR=./logs/audit