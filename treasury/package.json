{
  "name": "treasury",
  "version": "0.1.0",
  "description": "User App — human-facing interaction/intent context for the Ledger ecosystem",
  "type": "commonjs",
  "scripts": {
    "dev": "trap 'kill 0' EXIT; npm run dev:backend & npm run dev:client & wait",
    "dev:backend": "tsx watch src/index.ts",
    "dev:client": "npm --prefix src/presentation/web/client run dev",
    "start": "node dist/index.js",
    "build": "tsc && mkdir -p dist/presentation/web && cp -r src/presentation/web/client dist/presentation/web/",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed:directory": "tsx src/infra/seeding/run-seed.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "mongodb": "^7.5.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^25.3.3",
    "tsx": "^4.21.0",
    "typescript": "^5.4.5",
    "vitest": "^4.1.0"
  }
}
